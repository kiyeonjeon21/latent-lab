defaults:
  - base
  - _self_

name: llm_prompting_compare
description: "Compare zero-shot, few-shot, and chain-of-thought prompting"
domain: llm
task: prompting

model:
  pretrained: mlx-community/Llama-3.2-3B-Instruct-4bit

prompt: "What is 15% of 240?"
max_tokens: 256

tags: [llm, prompting, compare]

defaults:
  - base
  - _self_

name: llm_inference_compare
description: "Compare inference across multiple MLX models"
domain: llm
task: inference

model:
  pretrained: mlx-community/Llama-3.2-3B-Instruct-4bit

models:
  - mlx-community/Llama-3.2-3B-Instruct-4bit
  - mlx-community/Qwen2.5-7B-Instruct-4bit

prompt: "Explain the concept of attention in transformers briefly."
max_tokens: 256
temperature: 0.7

tags: [llm, inference, compare]
